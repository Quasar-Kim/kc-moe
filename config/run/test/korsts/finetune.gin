include 't5x/examples/t5/t5_1_1/base.gin'
include 't5x/configs/runs/finetune.gin'
include 'config/run/finetune_default.gin'

# 2^17 tokens(2^9 * 2^8) tokens per step
# dataset has approx. 280,000 tokens
# -> 2 steps = one epochs
# train for 128 epochs ->. 256 steps
TASK_FEATURE_LENGTHS = {"inputs": 512, "targets": 64}
TRAIN_STEPS = 25_256 # 256 + 25_000
MIXTURE_OR_TASK_NAME = 'korsts'
INITIAL_CHECKPOINT_PATH = 'gs://kc-moe/t5x/kct5-base/checkpoint_25000'
MODEL_DIR = 'gs://kc-moe/t5x/kct5-base-korsts'
EVAL_PERIOD = 2
CHECKPOINT_PERIOD = 4